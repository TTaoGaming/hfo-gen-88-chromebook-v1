// Medallion: Bronze | Mutation: 0% | HIVE: V
import { defineConfig, devices } from '@playwright/test';

export default defineConfig({
    testDir: './scripts',
    fullyParallel: false, // Maintain order for mission-critical tests
    forbidOnly: !!process.env.CI,
    retries: 2,
    workers: 1, // Restricted for Chromebook performance
    reporter: 'list',
    use: {
        trace: 'on-first-retry',
        screenshot: 'only-on-failure',
    },
    projects: [
        {
            name: 'chromium',
            use: {
                ...devices['Desktop Chrome'],
                // Try to use bundled chromium first, fallback to system if needed
                // channel: 'chrome', 
            },
        },
    ],
});
